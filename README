The XCParser module contains a parser (file) for .XCompose files and types for
the representation.

The Extended module contains the same, with extensions to the syntax (and
correspondingly to the representation types). Currently, there is one extension:
Grouping with {} allows common prefixes to be factored out.

